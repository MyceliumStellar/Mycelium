[pytest]
# Import test modules by file path (avoids top-level package-name clashes
# between sdk/tests and cli/tests).
addopts = --import-mode=importlib
testpaths = sdk/tests cli/tests compiler/tests
